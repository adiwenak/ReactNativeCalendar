import { StyleSheet } from "react-native"

export default StyleSheet.create({
    bottomOuterView: {
        flex: 1,
        justifyContent: "space-around",
        flexDirection: "row",
        alignItems: "center"
    }
})
